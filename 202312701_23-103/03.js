const answer = `require() 함수 를 사용하여 모듈을 불러오면된다 
const myLocalModule = require('모듈파일의 주소'); 모듈을 불러오기 위해서는 require() 함수에 불러올 모듈 파일의 주소를 인자로 넣어주면 된다.`;
//chai 를 가져오려면 다음을 호출하십시오.
//var crypto = require('chai');
console.log(answer);